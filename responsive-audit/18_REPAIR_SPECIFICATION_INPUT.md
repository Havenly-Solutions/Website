# 18 Repair Specification & Maintenance Input

1. Maintain Mobile-First CSS Order in `app/globals.css`.
2. Do not append un-nested desktop styles at the bottom of CSS files.
3. Keep all interactive buttons at minimum $44\\text{px}$ touch target height.
4. Ensure all new form inputs use `16px` base font size.
